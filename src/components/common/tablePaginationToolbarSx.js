/** Shared TablePagination toolbar layout so numbered actions stay on one row. */
export const tablePaginationToolbarSx = {
  "& .MuiTablePagination-toolbar": {
    flexWrap: "nowrap",
    gap: 1,
  },
};
